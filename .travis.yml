language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t snirhazan/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true snirhazan/docker-react npm run test